[build]
dockerfilePath = "Dockerfile.prod"

[deploy]
startCommand = "python manage.py collectstatic --no-input && python manage.py migrate && gunicorn config.wsgi --workers 2 --bind 0.0.0.0:8000"
healthcheckPath = "/cms/login/"
healthcheckTimeout = 300
