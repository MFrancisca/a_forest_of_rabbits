[build]
dockerfilePath = "Dockerfile.prod"

[deploy]
startCommand = "python manage.py migrate --no-input && gunicorn config.wsgi --workers 2 --bind 0.0.0.0:$PORT"
healthcheckPath = "/cms/login/"
healthcheckTimeout = 600
