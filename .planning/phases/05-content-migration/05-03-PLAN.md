---
phase: 05-content-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified: []
autonomous: false
requirements:
  - INFRA-03

must_haves:
  truths:
    - "Every article from the current Google Sites is readable on the new site"
    - "Images from migrated articles display correctly — no broken links pointing to Google Sites"
    - "No content that exists on the current Google Sites is missing from the new site"
    - "The three sign-off checkboxes in migration-checklist.md are all ticked"
  artifacts:
    - path: "docs/migration-checklist.md"
      provides: "Phase completion evidence — all rows ticked, sign-off checkboxes ticked"
      contains: "All article rows [x] AND the three sign-off checkboxes [x]"
  key_links:
    - from: "docs/migration-checklist.md"
      to: "Phase 6"
      via: "Checklist 100% complete is the gate condition for Phase 6 DNS cutover"
      pattern: "completion-gate"
---

<objective>
Final review and sign-off: side-by-side comparison of the live Google Sites against the new Wagtail site, confirmation that all three sign-off checkboxes in the checklist are ticked, and the existing test suite is green. When this plan completes, Phase 5 is done and Phase 6 (Production Deploy) is unblocked.

Purpose: Catch anything missed before DNS cutover. A single missed article or broken image on the live site is much harder to fix post-launch.
Output: Confirmed complete migration; Phase 5 marked done; Phase 6 unblocked.
</objective>

<execution_context>
@/home/mariefran/.claude/get-shit-done/workflows/execute-plan.md
@/home/mariefran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/migration-checklist.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Side-by-side review and sign-off</name>
  <action>Human-performed review — see how-to-verify below. Claude has no automated actions in this task.</action>
  <verify>
    <automated>MISSING — visual review required. Verified manually: all three sign-off checkboxes ticked, docker compose exec web pytest -q green, checklist committed.</automated>
  </verify>
  <done>Three sign-off checkboxes ticked in migration-checklist.md; no content missing from new site; existing test suite green; Phase 6 unblocked</done>
  <what-built>All Google Sites articles have been migrated into Wagtail and the checklist is filled in (Plan 02 complete).</what-built>
  <how-to-verify>
Open two browser windows side by side:
- **Left:** The live Google Sites at www.unanuovasperanza.art — navigate through Calligraphy & Illumination, Clothing, and Armor sections
- **Right:** The new Wagtail site at http://localhost:8000/projects/

**Article-by-article check:**
For each article on Google Sites:
1. Find the corresponding article in the Wagtail site checklist
2. Open the Wagtail article at its /projects/<slug>/ URL
3. Confirm: title matches, body text is present and readable, all images display (not broken), formatting is acceptable (minor differences OK)
4. If anything is missing or broken: fix it in /cms/ now, then continue

**When satisfied everything is present and correct:**
Open `docs/migration-checklist.md` and tick the three sign-off checkboxes:
- [x] All rows above are ticked
- [x] Side-by-side browser comparison with live Google Sites done — no missing content
- [x] Existing test suite green: `docker compose exec web pytest -q`

Run the test suite to confirm it is still green:
```
docker compose exec web pytest -q
```

Commit the final checklist state:
```
git add docs/migration-checklist.md && git commit -m "docs(05): sign off content migration — phase 5 complete"
```
  </how-to-verify>
  <resume-signal>Type "approved" when the side-by-side review is complete, all three sign-off checkboxes are ticked, the test suite is green, and the checklist is committed</resume-signal>
</task>

</tasks>

<verification>
Phase 5 is complete when:

1. docs/migration-checklist.md has all article rows ticked [x] AND all three sign-off checkboxes ticked [x]
2. Every /projects/<slug>/ URL from the checklist loads with body text and images rendering correctly
3. `docker compose exec web pytest -q` passes with no failures
4. Final checklist commit appears in git log
</verification>

<success_criteria>
- Side-by-side comparison confirms no Google Sites article is absent from the new site
- All images in all migrated articles display correctly (no broken links)
- Three sign-off checkboxes in migration-checklist.md are all ticked [x]
- Existing test suite green
- Phase 6 (Production Deploy) is now unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-migration/05-03-SUMMARY.md`

Also update `.planning/STATE.md` to reflect Phase 5 complete.
</output>
