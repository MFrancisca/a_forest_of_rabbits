---
phase: 05.1-google-sites-scraper-and-importer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements/base.txt
  - Dockerfile
  - blog/management/commands/scrape_google_sites.py
  - blog/tests/test_scrape_google_sites.py
autonomous: true
requirements:
  - INFRA-03
must_haves:
  truths:
    - "Playwright, beautifulsoup4, and lxml are installable from requirements/base.txt"
    - "Dockerfile rebuilds with Chromium binary available inside the container"
    - "Nine test stubs exist and are collectable by pytest (all fail with NotImplementedError or similar)"
    - "scrape_google_sites.py stub exists so test imports do not raise ImportError"
  artifacts:
    - path: "requirements/base.txt"
      provides: "playwright>=1.40, beautifulsoup4>=4.12, lxml>=5.0 entries"
      contains: "playwright"
    - path: "Dockerfile"
      provides: "Chromium binary install step"
      contains: "playwright install"
    - path: "blog/management/commands/scrape_google_sites.py"
      provides: "Command stub importable by tests"
    - path: "blog/tests/test_scrape_google_sites.py"
      provides: "8 failing test stubs"
      min_lines: 40
  key_links:
    - from: "blog/tests/test_scrape_google_sites.py"
      to: "blog/management/commands/scrape_google_sites.py"
      via: "from blog.management.commands.scrape_google_sites import ..."
      pattern: "import.*scrape_google_sites"
---

<objective>
Install Playwright/BeautifulSoup dependencies, update Dockerfile for Chromium, and create the Wave 0 test scaffold.

Purpose: Nothing can be implemented or tested until the libraries exist and tests are wired to the stub command. This plan unblocks all subsequent plans.
Output: Updated requirements/base.txt, updated Dockerfile, importable command stub, 8 failing test stubs.
</objective>

<execution_context>
@/home/mariefran/.claude/get-shit-done/workflows/execute-plan.md
@/home/mariefran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-google-sites-scraper-and-importer/05.1-CONTEXT.md
@.planning/phases/05.1-google-sites-scraper-and-importer/05.1-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs. -->

From blog/management/commands/create_site_skeleton.py (reference pattern):
```python
from django.core.management.base import BaseCommand
from wagtail.models import Page

class Command(BaseCommand):
    help = '...'
    def handle(self, *args, **options):
        ...
        parent.add_child(instance=page)
        page.save_revision().publish()
```

From blog/models.py (target models for tests):
- ProjectPage: title, slug, date, category (FK to Category), excerpt, body (RichTextField)
- AboutPage: body (RichTextField), profile_photo (unused)
- Category: name
- ProjectsIndexPage: parent for all ProjectPages
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies to requirements/base.txt and Dockerfile</name>
  <files>requirements/base.txt, Dockerfile</files>
  <action>
    In requirements/base.txt, append three new lines after the existing entries:
    ```
    playwright>=1.40
    beautifulsoup4>=4.12
    lxml>=5.0
    ```

    In Dockerfile, add one RUN line immediately after the existing `RUN pip install --no-cache-dir -r requirements/dev.txt` line:
    ```dockerfile
    RUN python -m playwright install --with-deps chromium
    ```

    Do NOT add `requests` — it is not needed (Playwright handles fetching; requests was the original CONTEXT.md approach that research overrode).

    Note: The Dockerfile install uses `--with-deps` to include OS-level Chromium dependencies (libnss3, libgbm, etc.) in the python:3.12-slim image. This adds ~130 MB to the image but is required for headless operation.
  </action>
  <verify>
    <automated>grep -c "playwright\|beautifulsoup4\|lxml" /home/mariefran/Projects/Website/requirements/base.txt && grep "playwright install" /home/mariefran/Projects/Website/Dockerfile</automated>
  </verify>
  <done>requirements/base.txt has 3 new entries; Dockerfile has `RUN python -m playwright install --with-deps chromium` after the pip install line</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create command stub and 8 failing test stubs (RED)</name>
  <files>blog/management/commands/scrape_google_sites.py, blog/tests/test_scrape_google_sites.py</files>
  <behavior>
    - test_extract_title_from_h1: extract_title(html) returns H1 text when H1 is present
    - test_extract_title_fallback_to_title_tag: extract_title(html) parses title tag "Site - Classes" -> "Classes" when no H1
    - test_extract_body_paragraphs: extract_body_html(html, slug) wraps paragraphs in &lt;p&gt; tags
    - test_extract_body_headings: extract_body_html(html, slug) converts h2 text to &lt;h2&gt;text&lt;/h2&gt;
    - test_extract_body_image_placeholders: extract_body_html(html, slug) inserts `&lt;p&gt;🖼️ [IMAGE: my-slug-1]&lt;/p&gt;` at each img location, counter increments
    - test_slug_from_url_basic: slug_from_url("https://www.unanuovasperanza.art/projects/scrolls/qc-sonja-iii") -> "qc-sonja-iii"
    - test_create_project_page_creates_page: given ProjectsIndexPage in DB, create_project_page() creates a published ProjectPage (uses @pytest.mark.django_db)
    - test_create_project_page_skips_existing: if slug already exists, create_project_page() returns 'skipped' and does not duplicate (uses @pytest.mark.django_db)
    - test_update_about_page: update_about_page(html) overwrites AboutPage.body and publishes the revision (uses @pytest.mark.django_db; asserts body contains extracted text)
  </behavior>
  <action>
    1. Create `blog/management/commands/scrape_google_sites.py` as a minimal stub:
       - Define the module-level functions with `raise NotImplementedError` bodies: `extract_title`, `extract_body_html`, `slug_from_url`, `create_project_page`, `update_about_page`
       - Define the `Command` class (subclass BaseCommand) with `help` string and empty `handle` method that calls `pass`
       - This stub must be importable without error (no Playwright calls at module level)

    2. Create `blog/tests/test_scrape_google_sites.py` with all 9 test stubs:
       - Import the helper functions: `from blog.management.commands.scrape_google_sites import extract_title, extract_body_html, slug_from_url, create_project_page`
       - For pure-function tests (test_extract_title_*, test_extract_body_*, test_slug_from_url_basic): write minimal fixture HTML strings inline, call the function, assert expected output
       - For DB tests (test_create_project_page_*): use `@pytest.mark.django_db` decorator; use existing conftest fixtures or factory_boy to set up a ProjectsIndexPage parent
       - All tests must FAIL in the RED state (functions raise NotImplementedError)

    Fixture HTML example for title tests:
    ```python
    TITLE_HTML = '<html><body><h1>Queens Champion for Sonja III</h1><p>body</p></body></html>'
    FALLBACK_HTML = '<html><head><title>Site - Classes</title></head><body><p>no h1</p></body></html>'
    ```

    Run `pytest blog/tests/test_scrape_google_sites.py --collect-only -q` to verify collection succeeds (9 tests collected).
    Run `pytest blog/tests/test_scrape_google_sites.py -x -q` to confirm tests fail (not ERROR).
  </action>
  <verify>
    <automated>cd /home/mariefran/Projects/Website && pytest blog/tests/test_scrape_google_sites.py --collect-only -q 2>&1 | grep "9 tests collected"</automated>
  </verify>
  <done>9 tests collected; all fail with NotImplementedError (RED state confirmed); includes test_update_about_page; no ImportError or collection error</done>
</task>

</tasks>

<verification>
- `grep -c "playwright\|beautifulsoup4\|lxml" requirements/base.txt` returns 3
- `grep "playwright install" Dockerfile` matches
- `pytest blog/tests/test_scrape_google_sites.py --collect-only -q` shows "9 tests collected"
- `pytest blog/tests/test_scrape_google_sites.py -x -q` exits non-zero (all RED)
</verification>

<success_criteria>
Wave 0 complete: dependencies declared, Dockerfile updated, command stub importable, 9 test stubs in RED state. Plan 02 (implementation) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-google-sites-scraper-and-importer/05.1-01-SUMMARY.md`
</output>
