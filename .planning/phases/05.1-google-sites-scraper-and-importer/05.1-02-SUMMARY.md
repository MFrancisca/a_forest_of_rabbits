---
phase: 05.1-google-sites-scraper-and-importer
plan: "02"
subsystem: infra
tags: [beautifulsoup4, lxml, django-management-command, tdd, pytest, wagtail]

# Dependency graph
requires:
  - phase: 05.1-01
    provides: scrape_google_sites.py importable stub with NotImplementedError bodies; 9 failing test stubs in RED state
provides:
  - extract_title(html) — BeautifulSoup H1 extraction with <title> fallback
  - extract_body_html(html, slug) — paragraph/heading/image-placeholder extraction
  - slug_from_url(url) — last path segment via urlparse
  - create_project_page(title, slug, body_html, date, category_name) — idempotent Wagtail page creation
  - update_about_page(html) — AboutPage body overwrite with save_revision().publish()
affects:
  - 05.1-03 (Plan 03 wires these helpers together in Command.handle())

# Tech tracking
tech-stack:
  added: [lxml installed into .venv (was declared in requirements/base.txt but not installed)]
  patterns: [TDD GREEN — replace NotImplementedError stubs with real BeautifulSoup/urlparse/Wagtail implementations]

key-files:
  created: []
  modified:
    - blog/management/commands/scrape_google_sites.py

key-decisions:
  - "lxml was declared in requirements/base.txt (Plan 01) but not installed in .venv — auto-fixed by running pip install lxml (Rule 3: blocking)"
  - "create_project_page signature follows the test stub contract (title, slug, body_html, date=None, category_name=None) — RESEARCH.md shows a different signature intended for Plan 03 command use; tests define the unit-testable interface"
  - "update_about_page calls extract_body_html internally rather than accepting pre-extracted HTML — test passes html string directly as specified in test_update_about_page"

patterns-established:
  - "Model imports inside function bodies (from blog.models import ...) — avoids circular import issues when module is loaded before Django app registry is ready"
  - "BeautifulSoup lxml parser: use 'lxml' not 'html.parser' for real-world HTML robustness; lxml must be pip-installed separately from beautifulsoup4"

requirements-completed: [INFRA-03]

# Metrics
duration: 1min
completed: 2026-03-10
---

# Phase 5.1 Plan 02: Google Sites Scraper Helper Functions Summary

**Five BeautifulSoup/urlparse/Wagtail helper functions implemented in scrape_google_sites.py — all 9 TDD tests pass GREEN, full 58-test suite green**

## Performance

- **Duration:** 1 min
- **Started:** 2026-03-10T20:10:29Z
- **Completed:** 2026-03-10T20:12:08Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Implemented `extract_title` using BeautifulSoup lxml parser — prefers H1, falls back to `<title>` tag with site-name prefix stripping
- Implemented `extract_body_html` traversing h2/h3/p/img tags in document order, inserting `slug-N` image placeholders
- Implemented `slug_from_url` via `urlparse().path` last segment extraction
- Implemented `create_project_page` with idempotency guard (`slug exists -> return 'skipped'`) using `add_child + save_revision().publish()`
- Implemented `update_about_page` calling `extract_body_html` then saving AboutPage revision

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement all 5 helper functions (GREEN)** - `948a78d` (feat)

**Plan metadata:** _(docs commit follows)_

_Note: TDD GREEN phase — Plan 01 was the RED scaffold; this plan turns all 9 stubs green._

## Files Created/Modified

- `blog/management/commands/scrape_google_sites.py` — Replaced 5 NotImplementedError stubs with real implementations using BeautifulSoup, urlparse, and Wagtail ORM patterns

## Decisions Made

- `lxml` was declared in `requirements/base.txt` (Plan 01) but not installed in `.venv` — auto-fixed with `pip install lxml` (Rule 3: blocking issue)
- `create_project_page` signature kept as `(title, slug, body_html, date=None, category_name=None)` to match test contract — RESEARCH.md shows a different signature intended for Plan 03's `Command.handle()` loop; the unit test interface is the contract here
- `update_about_page` accepts raw HTML and calls `extract_body_html` internally — matches test expectation of `update_about_page(html)` with a full HTML string

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] lxml parser not installed in .venv**
- **Found during:** Task 1 (first test run after implementing extract_title)
- **Issue:** `bs4.exceptions.FeatureNotFound: Couldn't find a tree builder with the features you requested: lxml` — package declared in requirements/base.txt but not pip-installed in development venv
- **Fix:** Ran `.venv/bin/pip install lxml` — lxml 6.0.2 installed
- **Files modified:** None (venv only, requirements/base.txt already had the declaration)
- **Verification:** Re-ran all 9 tests — 9 passed
- **Committed in:** 948a78d (included in task commit)

---

**Total deviations:** 1 auto-fixed (1 blocking install)
**Impact on plan:** Essential to unblock tests; requirements/base.txt was already correct. No scope creep.

## Issues Encountered

None — after auto-fixing the missing lxml install, all 9 tests passed on the first implementation attempt.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 03 (command wiring — `Command.handle()` with Playwright fetch loop) is fully unblocked
- All 5 helper functions are importable and tested
- Full 58-test suite passes green
- lxml is now available in dev venv; Docker image already has it declared in requirements/base.txt (will install during image build)

---
*Phase: 05.1-google-sites-scraper-and-importer*
*Completed: 2026-03-10*
