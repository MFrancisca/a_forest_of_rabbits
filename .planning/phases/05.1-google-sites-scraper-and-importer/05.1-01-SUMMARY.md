---
phase: 05.1-google-sites-scraper-and-importer
plan: "01"
subsystem: infra
tags: [playwright, beautifulsoup4, lxml, django-management-command, tdd, pytest]

# Dependency graph
requires:
  - phase: 04-editorial-content
    provides: blog models (ProjectPage, AboutPage, ProjectsIndexPage), factories, conftest fixtures
provides:
  - playwright>=1.40, beautifulsoup4>=4.12, lxml>=5.0 declared in requirements/base.txt
  - Dockerfile Chromium install step (python -m playwright install --with-deps chromium)
  - scrape_google_sites.py importable command stub with 5 NotImplementedError functions
  - 9 pytest test stubs in RED state (all fail NotImplementedError)
affects:
  - 05.1-02 (implementation plan — these stubs are the contract it must green)
  - Dockerfile builds for production/dev containers

# Tech tracking
tech-stack:
  added: [playwright>=1.40, beautifulsoup4>=4.12, lxml>=5.0]
  patterns: [TDD RED scaffold — stub imports at module level, NotImplementedError bodies, test stubs verify contract before implementation]

key-files:
  created:
    - blog/management/commands/scrape_google_sites.py
    - blog/tests/test_scrape_google_sites.py
  modified:
    - requirements/base.txt
    - Dockerfile

key-decisions:
  - "Do NOT add requests — Playwright handles fetching; requests was the original CONTEXT.md approach that research overrode"
  - "Dockerfile Chromium install uses --with-deps to include OS-level deps (libnss3, libgbm, etc.) needed for headless operation in python:3.12-slim"
  - "9 test stubs (not 8) — task list specified 8 but behavior spec listed 9 distinct behaviors including test_update_about_page"

patterns-established:
  - "TDD RED scaffold: stub file importable at module level, all functions raise NotImplementedError, tests import and exercise those stubs"
  - "DB tests use existing conftest fixtures (projects_index, about_page) from conftest.py — no new fixtures needed"

requirements-completed: [INFRA-03]

# Metrics
duration: 2min
completed: 2026-03-10
---

# Phase 5.1 Plan 01: Google Sites Scraper Wave 0 — Dependencies and Test Scaffold Summary

**playwright, beautifulsoup4, and lxml added to requirements/base.txt; Dockerfile updated for Chromium; scrape_google_sites.py stub importable with 9 failing test stubs in RED state**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-10T20:06:22Z
- **Completed:** 2026-03-10T20:08:10Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Declared Playwright, BeautifulSoup4, and lxml dependencies in requirements/base.txt (unblocks pip install)
- Added `RUN python -m playwright install --with-deps chromium` to Dockerfile (provides headless Chromium for scraping)
- Created importable `scrape_google_sites.py` stub with 5 functions and Django management Command class
- Created 9 failing test stubs covering all planned helper functions — all fail NotImplementedError (RED state confirmed)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add dependencies to requirements/base.txt and Dockerfile** - `739af86` (chore)
2. **Task 2: Create command stub and 9 failing test stubs (RED)** - `053d9ba` (test)

**Plan metadata:** _(docs commit follows)_

_Note: TDD RED phase — Task 2 is the test commit only; GREEN/REFACTOR commits come in Plan 02._

## Files Created/Modified

- `requirements/base.txt` — Added playwright>=1.40, beautifulsoup4>=4.12, lxml>=5.0
- `Dockerfile` — Added RUN python -m playwright install --with-deps chromium after pip install step
- `blog/management/commands/scrape_google_sites.py` — Importable stub: extract_title, extract_body_html, slug_from_url, create_project_page, update_about_page (all NotImplementedError), Command class with empty handle()
- `blog/tests/test_scrape_google_sites.py` — 9 test stubs: 6 pure-function tests, 3 @pytest.mark.django_db tests

## Decisions Made

- Do NOT add `requests` — Playwright handles fetching; requests was the original CONTEXT.md approach that research overrode
- Dockerfile Chromium install uses `--with-deps` to include OS-level dependencies (libnss3, libgbm, etc.) required for headless operation in python:3.12-slim (+~130 MB to image, required)
- Plan mentioned "8 failing test stubs" in the task name but the behavior spec listed 9 distinct behaviors including `test_update_about_page` — 9 stubs created to match the behavior spec (the done criteria also says "9 tests collected")

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None — collection and RED verification both passed cleanly. The `--collect-only` output shows 9 tests collected; running without `--no-cov` causes a coverage threshold failure (80% not reached) but that is pre-existing project configuration unrelated to this plan.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 02 (implementation) is fully unblocked: dependencies declared, Dockerfile updated, stub importable, test contract defined
- All 9 tests fail with NotImplementedError — Plan 02 must turn them GREEN
- Coverage threshold failure is pre-existing; Plan 02 may need `--no-cov` or a `--cov-fail-under` adjustment depending on how implementation coverage is measured

---
*Phase: 05.1-google-sites-scraper-and-importer*
*Completed: 2026-03-10*

## Self-Check: PASSED

All files present and both task commits verified.
