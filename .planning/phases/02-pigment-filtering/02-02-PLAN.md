---
phase: 02-pigment-filtering
plan: "02"
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - pigments/views.py
  - pigments/templates/pigments/pigment_list.html
  - pigments/templates/pigments/pigment_list_partial.html
autonomous: true
requirements: [PIGM-01, TEST-03, TEST-04]

must_haves:
  truths:
    - "Visiting /pigments/ returns a full HTML page with filter controls and a pigment list"
    - "Sending HX-Request: true returns only the list fragment (no base.html wrapper)"
    - "Sending HX-Request: true AND HX-History-Restore-Request: true returns the full page"
    - "Filtering by color family returns only matching pigments"
    - "Filtering by two color families (?color=1&color=2) returns pigments from either (OR within dimension)"
    - "Filtering by color AND country returns only pigments matching BOTH (AND across dimensions)"
    - "Filtering by century ?century=800 returns pigments whose manuscripts overlap 800-899"
    - "A pigment linked to 2 manuscripts in the same country appears exactly once in filtered results"
    - "Filtering with no matches returns status 200 with filtered_count == 0 (not a 404)"
    - "All 11 tests in test_views.py pass green"
  artifacts:
    - path: "pigments/views.py"
      provides: "pigment_list view — filtering, HTMX detection, century buckets, active pills"
      exports: ["pigment_list", "get_century_buckets", "build_active_pills", "build_filtered_queryset"]
    - path: "pigments/templates/pigments/pigment_list.html"
      provides: "Full page — extends base.html; includes filter bar, active pills row, result count, pigment list rows"
    - path: "pigments/templates/pigments/pigment_list_partial.html"
      provides: "HTMX fragment — active pills row, result count, pigment list rows; NO extends"
  key_links:
    - from: "pigments/templates/pigments/pigment_list.html"
      to: "pigments/templates/pigments/pigment_list_partial.html"
      via: "{% include 'pigments/pigment_list_partial.html' %}"
      pattern: "include.*pigment_list_partial"
    - from: "pigments/templates/pigments/pigment_list.html (filter form)"
      to: "pigments/views.pigment_list"
      via: "hx-get='/pigments/' hx-target='#pigment-results' hx-push-url='true'"
    - from: "pigments/views.pigment_list"
      to: "pigments/views.build_filtered_queryset"
      via: "function call with request.GET"
    - from: "pigments/views.build_filtered_queryset"
      to: "Pigment.objects (M2M joins)"
      via: "manuscript_links__manuscript__country__id__in and .distinct()"
---

<objective>
Implement the pigment_list view and both templates (full-page + partial). Turn all 11 failing tests GREEN.

Purpose: This is the GREEN phase — the working filterable pigment list at /pigments/. View handles three request types (direct, HTMX, HTMX history restore), all five filter dimensions with correct OR-within/AND-across logic, and the consecutive-century pill merge.
Output: Complete view + two templates. All 11 tests pass.
</objective>

<execution_context>
@/home/mariefran/.claude/get-shit-done/workflows/execute-plan.md
@/home/mariefran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-pigment-filtering/02-CONTEXT.md
@.planning/phases/02-pigment-filtering/02-RESEARCH.md
@.planning/phases/02-pigment-filtering/02-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From pigments/models.py:
- Pigment.objects.select_related("color_family", "pigment_family")
- Pigment.color_family → ColorFamily (FK, nullable=True)
- Pigment.pigment_family → PigmentFamily (FK, nullable=True)
- Pigment.manuscript_links → reverse FK to PigmentManuscript
- PigmentManuscript.manuscript → Manuscript (FK)
- Manuscript.country → Country (FK)
- Manuscript.date_start, Manuscript.date_end → IntegerField (nullable)

From pigments/factories.py (all zero-arg callable):
- ColorFamilyFactory(), PigmentFamilyFactory(), CountryFactory()
- ManuscriptFactory(date_start=800, date_end=899)  ← pass dates explicitly for century tests
- PigmentFactory(), PigmentManuscriptFactory(pigment=p, manuscript=ms)

HTMX header detection (use request.headers dict, NOT request.META):
- is_htmx = request.headers.get("HX-Request") == "true"
- is_history_restore = request.headers.get("HX-History-Restore-Request") == "true"
- Return partial ONLY when: is_htmx and not is_history_restore

Django test client passes HTMX headers as:
- HTTP_HX_REQUEST="true"
- HTTP_HX_HISTORY_RESTORE_REQUEST="true"

CRITICAL — SQLite test compatibility:
- Use plain .distinct() — NOT .distinct("field") (DISTINCT ON not supported by SQLite)

URL query param names (Claude's discretion — use these for consistency with test_views.py stubs):
- ?color=  (color family IDs)
- ?pfam=   (pigment family IDs)
- ?country= (country IDs)
- ?manuscript= (manuscript IDs)
- ?century= (century start year, e.g. 800 for 9th century)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (GREEN): Implement pigment_list view</name>
  <files>pigments/views.py</files>
  <behavior>
    The view must satisfy these behaviors (tests already written in 02-01):

    - test_pigment_list_returns_200: plain GET returns 200
    - test_unfiltered_list_shows_all: context["filtered_count"] == total pigment count when no params
    - test_color_family_filter: ?color=id → only pigments with that color_family in results
    - test_multivalue_or_within_dimension: ?color=id1&color=id2 → pigments from EITHER family
    - test_and_across_dimensions: ?color=X&country=Y → pigments matching BOTH (not just one)
    - test_century_overlap_filter: ?century=800 → pigments whose manuscript date range overlaps 800-899
    - test_empty_results_filter: ?color=99999 → 200 status, filtered_count == 0
    - test_combined_filter_m2m_no_duplicates: pigment with 2 manuscripts in same country → filtered_count == 1
    - test_htmx_request_returns_partial: HTTP_HX_REQUEST="true" → renders pigment_list_partial.html
    - test_history_restore_returns_full_page: HTTP_HX_REQUEST="true" + HTTP_HX_HISTORY_RESTORE_REQUEST="true" → renders pigment_list.html
    - test_direct_get_returns_full_page: no headers → renders pigment_list.html

    Required context keys passed to templates:
    - "pigments": filtered queryset (distinct)
    - "filtered_count": len(qs) or qs.count()
    - "total_count": Pigment.objects.count()
    - "is_filtered": bool — True if any filter param is set
    - "color_families": ColorFamily.objects.all()
    - "pigment_families": PigmentFamily.objects.all()
    - "countries": Country.objects.all()
    - "manuscripts": Manuscript.objects.all()
    - "century_buckets": result of get_century_buckets()
    - "active_pills": result of build_active_pills(request.GET)
    - "active_filters": request.GET  (raw params for template re-selection state)

    Helper functions (in same file):
    - get_century_buckets() — see RESEARCH.md Pattern 3
    - build_filtered_queryset(params) — see RESEARCH.md Pattern 2; always ends with .distinct()
    - build_active_pills(params) — see RESEARCH.md Pattern 6; handles consecutive century merge
    - century_label(c_start) — converts 800 → "9th century"; use ordinals dict from RESEARCH.md
  </behavior>
  <action>
    Implement pigments/views.py using the complete view structure from RESEARCH.md "Code Examples" section. The file currently contains a stub. Replace it with the full implementation.

    Key implementation rules:
    1. Use request.headers.get("HX-Request") — NOT request.META["HTTP_HX_REQUEST"]
    2. Always call .distinct() at end of build_filtered_queryset — never skip it
    3. Use request.GET.getlist("color") — NOT request.GET.get("color")
    4. For century overlap: date_start__lte=(c_start+99) AND date_end__gte=c_start
    5. build_active_pills must merge consecutive centuries into a single pill (e.g. 800+900 → "9th–10th centuries")
    6. get_century_buckets must skip centuries with no manuscript data (no empty buckets)

    After implementing, run the test suite to confirm GREEN:
    python -m pytest pigments/tests/test_views.py -x -q

    Commit message: "feat(02-02): implement pigment_list view — filter logic and HTMX detection"
  </action>
  <verify>
    <automated>cd /home/mariefran/Projects/Website && python -m pytest pigments/tests/test_views.py -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    All 11 tests in test_views.py pass. `python -m pytest pigments/tests/test_views.py -q` exits 0 with "11 passed".
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement pigment list templates</name>
  <files>pigments/templates/pigments/pigment_list.html, pigments/templates/pigments/pigment_list_partial.html</files>
  <action>
    Both stub templates currently contain minimal placeholder content. Replace with full implementations.

    === pigment_list.html (full page) ===
    - {% extends "core/templates/base.html" %} — match actual base template path
    - {% block content %} contains:
      a) Filter bar: horizontal flex row with five compact dropdowns (Color Family, Pigment Family, Country, Manuscript, Time Period)
      b) Active pills row: hidden when no pills, visible when {{ active_pills }} is non-empty
      c) {% include "pigments/pigment_list_partial.html" %} — result area wrapped in <div id="pigment-results">

    Filter bar structure (HTMX-driven):
    - Wrap all five dropdowns in <form id="filter-form" hx-get="/pigments/" hx-target="#pigment-results" hx-push-url="true" hx-trigger="change from:.filter-select">
    - Each dropdown: Alpine.js x-data component with open/close toggle, checkbox list panel, hidden <select multiple class="filter-select sr-only">
    - Alpine.js manages visible state; the hidden select drives HTMX (see RESEARCH.md Pattern 5)
    - Pre-select active values: for each filter dimension, check if the option ID is in active_filters.getlist(param)

    Active pills row (below filter bar, above results):
    - {% if active_pills %} ... {% endif %}
    - Each pill: label text + × button; clicking × removes that filter (HTMX or plain JS to uncheck the corresponding hidden select option and dispatch change event)
    - "Clear all" link alongside pills — navigates to /pigments/ (no params)

    Styling (Tailwind v3 CDN utilities — Claude's discretion for exact classes):
    - Filter bar: flex flex-wrap gap-2 mb-4
    - Dropdown button: border rounded px-3 py-1 text-sm
    - Dropdown panel: absolute z-10 bg-white border rounded shadow-md p-2 min-w-40
    - Pill: inline-flex items-center gap-1 bg-stone-100 border border-stone-300 rounded-full px-3 py-1 text-sm
    - Pill × button: text-stone-500 hover:text-stone-800

    === pigment_list_partial.html (HTMX fragment) ===
    CRITICAL: Must NOT contain {% extends %} — it is a bare fragment.
    Contains:
    - Result count: "{{ filtered_count }} of {{ total_count }} pigments" if is_filtered else "{{ total_count }} pigments"
    - Pigment list rows: {% for pigment in pigments %} ... {% empty %} ... {% endfor %}
    - Each row: pigment.name on left, "{{ pigment.color_family }} · {{ pigment.pigment_family }}" tags on right; link to /pigments/{{ pigment.pk }}/ (detail page, Phase 3)
    - Empty state: "No pigments match these filters." + <a href="/pigments/">Clear filters</a>

    The partial is included inside <div id="pigment-results"> in pigment_list.html. HTMX swaps the contents of that div on filter change.

    After implementing, run full test suite to confirm no regressions:
    python -m pytest pigments/ -q

    Commit message: "feat(02-02): implement pigment list templates — full page and partial fragment"
  </action>
  <verify>
    <automated>cd /home/mariefran/Projects/Website && python -m pytest pigments/ -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    Full test suite (pigments/) passes. Both templates exist with correct structure: pigment_list.html extends base.html and includes the partial; pigment_list_partial.html has NO extends directive. `curl -s http://localhost:8000/pigments/` (if dev server running) returns HTML containing "pigments".
  </done>
</task>

</tasks>

<verification>
- `python -m pytest pigments/tests/test_views.py -q` → 11 passed, 0 failed
- `python -m pytest pigments/ -q` → all tests pass (15 model tests + 11 view tests)
- pigment_list.html contains `{% extends` and `{% include "pigments/pigment_list_partial.html" %}`
- pigment_list_partial.html does NOT contain `{% extends`
- `grep -n "HX-Request\|HX-History" pigments/views.py` confirms both headers are checked
- `grep -n "\.distinct()" pigments/views.py` confirms .distinct() is called
- `grep -n "getlist" pigments/views.py` confirms all filter params use .getlist()
</verification>

<success_criteria>
All 11 view tests pass green. Full pigments/ test suite passes. The view correctly handles all three request types. Filter queryset always calls .distinct(). Templates render without TemplateDoesNotExist errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pigment-filtering/02-02-SUMMARY.md`
</output>
