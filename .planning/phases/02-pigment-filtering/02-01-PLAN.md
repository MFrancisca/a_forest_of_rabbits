---
phase: 02-pigment-filtering
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - pigments/tests/test_views.py
  - pigments/views.py
  - pigments/urls.py
  - pigments/templates/pigments/pigment_list.html
  - pigments/templates/pigments/pigment_list_partial.html
autonomous: true
requirements: [PIGM-01, TEST-03, TEST-04]

must_haves:
  truths:
    - "All 11 test functions exist in pigments/tests/test_views.py and are collected by pytest"
    - "pytest exits with 'collected 11 items' (or more); tests fail with assertion errors or missing template errors — NOT ImportError or ModuleNotFoundError"
    - "pigments/views.py exists and is importable with a pigment_list function"
    - "pigments/urls.py routes '' to views.pigment_list with name='list'"
    - "pigments/templates/pigments/pigment_list.html exists (minimal stub, extends base.html)"
    - "pigments/templates/pigments/pigment_list_partial.html exists (minimal stub, no extends)"
  artifacts:
    - path: "pigments/tests/test_views.py"
      provides: "11 test function stubs covering PIGM-01, TEST-03, TEST-04"
      exports: []
    - path: "pigments/views.py"
      provides: "pigment_list view stub"
      exports: ["pigment_list"]
    - path: "pigments/urls.py"
      provides: "URL route for /pigments/"
      contains: "path('', views.pigment_list, name='list')"
    - path: "pigments/templates/pigments/pigment_list.html"
      provides: "full-page template stub"
    - path: "pigments/templates/pigments/pigment_list_partial.html"
      provides: "partial fragment template stub"
  key_links:
    - from: "pigments/urls.py"
      to: "pigments/views.pigment_list"
      via: "path('', views.pigment_list, name='list')"
    - from: "config/urls.py"
      to: "pigments/urls.py"
      via: "path('pigments/', include('pigments.urls'))"
      note: "Already wired — no changes needed to config/urls.py"
---

<objective>
Write all failing test stubs for the pigment list view and create the minimum scaffolding (view stub, URL wiring, empty templates) so that `pytest` collects 11 tests and they fail with assertion errors rather than import or routing errors.

Purpose: Establish the RED phase. Every downstream plan depends on these test functions existing. Templates must exist as stubs so that `response.templates` checks don't crash with TemplateDoesNotExist before assertions run.
Output: 5 files — test_views.py (11 failing tests), views.py (stub), urls.py (route), two template stubs.
</objective>

<execution_context>
@/home/mariefran/.claude/get-shit-done/workflows/execute-plan.md
@/home/mariefran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pigment-filtering/02-CONTEXT.md
@.planning/phases/02-pigment-filtering/02-RESEARCH.md
@.planning/phases/02-pigment-filtering/02-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From pigments/factories.py (all callable with zero args):
- ColorFamilyFactory()
- PigmentFamilyFactory()
- CountryFactory()
- ManuscriptFactory()           # date_start / date_end are nullable — pass explicitly for century tests
- PigmentFactory()
- PigmentManuscriptFactory()    # links a Pigment to a Manuscript

From pigments/models.py (key fields for filter queries):
- Pigment: color_family (FK → ColorFamily), pigment_family (FK → PigmentFamily),
           manuscript_links (reverse relation → PigmentManuscript)
- PigmentManuscript: pigment (FK), manuscript (FK)
- Manuscript: country (FK → Country), date_start (IntegerField nullable), date_end (IntegerField nullable)

From config/urls.py (already wired):
- path('pigments/', include('pigments.urls'))  — no changes needed

From pigments/urls.py (currently empty, needs route added):
- app_name = 'pigments'
- urlpatterns = []  ← add path('', views.pigment_list, name='list')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing test stubs and scaffold files</name>
  <files>pigments/tests/test_views.py, pigments/views.py, pigments/urls.py, pigments/templates/pigments/pigment_list.html, pigments/templates/pigments/pigment_list_partial.html</files>
  <behavior>
    All 11 tests listed below must be collected and fail (not error on import). Test names are fixed — Plan 02 (GREEN) will make them pass without renaming.

    Tests to write (all in pigments/tests/test_views.py):
    1. test_pigment_list_returns_200 — GET /pigments/ → status 200
    2. test_unfiltered_list_shows_all — GET /pigments/ with 3 pigments created → context["filtered_count"] == 3
    3. test_color_family_filter — filter by color → only matching pigments in context
    4. test_multivalue_or_within_dimension — ?color=id1&color=id2 → pigments from either family returned
    5. test_and_across_dimensions — ?color=X&country=Y → only pigments matching BOTH
    6. test_century_overlap_filter — manuscript with date_start=820, date_end=870 → ?century=800 returns that pigment
    7. test_empty_results_filter — ?color=99999 → status 200, filtered_count == 0
    8. test_combined_filter_m2m_no_duplicates — pigment linked to 2 manuscripts in same country → ?country=id returns filtered_count == 1 (not 2)
    9. test_htmx_request_returns_partial — GET /pigments/ with HTTP_HX_REQUEST="true" → template "pigments/pigment_list_partial.html" used, NOT "pigments/pigment_list.html"
    10. test_history_restore_returns_full_page — GET /pigments/ with HTTP_HX_REQUEST="true" and HTTP_HX_HISTORY_RESTORE_REQUEST="true" → template "pigments/pigment_list.html" used
    11. test_direct_get_returns_full_page — GET /pigments/ (no HTMX headers) → template "pigments/pigment_list.html" used

    Stub implementations:
    - pigments/views.py: pigment_list function that returns HttpResponse("stub", status=200) — enough to make URL routing work
    - pigments/urls.py: add path('', views.pigment_list, name='list') to urlpatterns
    - pigments/templates/pigments/pigment_list.html: {% extends "base.html" %} {% block content %}stub{% endblock %}
    - pigments/templates/pigments/pigment_list_partial.html: <div>stub partial</div>  (NO extends)

    All tests must use @pytest.mark.django_db. All test data must be created via factories — no fixtures from conftest.py.

    For tests checking response.context["filtered_count"]: the stub view returns HttpResponse (not render), so context will be None — these tests will fail with AttributeError or KeyError, which is acceptable RED state. The important thing is they are collected and fail, not that they fail with a specific message.

    For template-checking tests (tests 9–11): use Django test Client, check response.templates. With the stub view returning HttpResponse directly (no render), response.templates will be empty — these tests will fail asserting the template name is present. Acceptable RED state.
  </behavior>
  <action>
    1. Create pigments/templates/pigments/ directory (mkdir -p).
    2. Write pigments/templates/pigments/pigment_list.html — minimal stub that extends base.html.
    3. Write pigments/templates/pigments/pigment_list_partial.html — minimal stub with NO extends.
    4. Write pigments/views.py — stub pigment_list that returns HttpResponse("stub", 200).
    5. Update pigments/urls.py — add the route (keep app_name = 'pigments', replace empty urlpatterns).
    6. Write pigments/tests/test_views.py — all 11 test functions using factory_boy + Django test Client.
    7. Run pytest pigments/tests/test_views.py -x -q — confirm 11 tests collected, all failing (not erroring on import). The -x flag will stop at the first failure but that is acceptable; what matters is no ImportError/ModuleNotFoundError at collection time.

    Commit message: "test(02-01): add failing view tests and scaffold stubs — RED"
  </action>
  <verify>
    <automated>cd /home/mariefran/Projects/Website && python -m pytest pigments/tests/test_views.py --collect-only -q 2>&amp;1 | grep "test session starts\|selected\|collected\|error" | head -20</automated>
  </verify>
  <done>
    `--collect-only` output shows "11 tests collected" (or "11 selected") with zero collection errors. Running `python -m pytest pigments/tests/test_views.py -q` shows 11 tests failing (FAILED), not erroring at import. All 5 scaffold files exist on disk.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest pigments/tests/test_views.py --collect-only -q` → 11 items, 0 errors
- `python -m pytest pigments/tests/test_views.py -q` → 11 FAILED (acceptable; RED phase)
- `python -m pytest pigments/tests/test_models.py -q` → existing 15 tests still pass (regression check)
- All 5 files exist: test_views.py, views.py, urls.py (updated), pigment_list.html, pigment_list_partial.html
</verification>

<success_criteria>
11 test functions are collected with no import errors. All 11 fail (RED). No regressions in test_models.py. Scaffold files exist and are importable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pigment-filtering/02-01-SUMMARY.md`
</output>
